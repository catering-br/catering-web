<p id="notice"><%= notice %></p>

<h1>Events</h1>

<table>
  <thead>
    <tr>
      <th>Consumer</th>
      <th>Event data</th>
      <th>Hour</th>
      <th>Attendants</th>
      <th>Endereço</th>
      <th>Duration</th>
      <th>Estimated price from</th>
      <th>Estimated price until</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.client %></td>
        <td><%= event.event_data %></td>
        <td><%= event.hour %></td>
        <td><%= event.attendants %></td>
        <td><%= event.address %></td>
        <td><%= event.duration %></td>
        <td><%= event.estimated_price_from %></td>
        <td><%= event.estimated_price_until %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
