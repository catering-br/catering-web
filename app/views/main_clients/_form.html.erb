<%= form_for(main_client) do |f| %>
  <% if main_client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_client.errors.count, "error") %> prohibited this main_client from being saved:</h2>

      <ul>
      <% main_client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Product %>
    <%= f.text_field :Product %>
  </div>

  <div class="field">
    <%= f.label :Quantidade %>
    <%= f.number_field :Quantidade %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
